"use client";

// Kept at its original URL for backward compatibility with existing links —
// renders the same authenticated legal-need selection screen as /get-started.
import { LegalNeedSelection } from "@/components/LegalNeedSelection";

export default function ReportIncidentPage() {
  return <LegalNeedSelection />;
}
